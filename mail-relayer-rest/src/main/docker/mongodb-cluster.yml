version: '2'
services:
  mailrelayer-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/mailrelayer-mongodb-config --bind_ip 0.0.0.0
  mailrelayer-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  mailrelayer-mongodb-config:
    image: mongo:4.2.7
    container_name: mailrelayer-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
